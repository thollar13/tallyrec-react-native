import { StyleSheet } from 'react-native'

const globalStyles = {
  breadcrumb: {
    marginTop: 64,
    height: 30,
    lineHeight: 30,
    backgroundColor: '#F0F0F0',
    textAlign: 'center',
  }
}

export default globalStyles