import * as LoggedIn from './authentication_action'

/// group all actions into this action creator constants
/// easier for larger applciations
export const ActionCreators = Object.assign({},
  LoggedIn,
)